apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: gitlab-config-job-runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: gitlab-config-job-runner
subjects:
- kind: ServiceAccount
  name: gitlab-config-job-runner
  namespace: openshift-gitops

